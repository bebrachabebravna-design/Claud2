/* =====================================================================
   ПРИМЕР. Скопируйте этот файл в assets/tg-config.js и впишите свои данные.
   Настоящий assets/tg-config.js НЕ хранится в репозитории (в нём токен бота).

   Зачем этот файл: если хостинг блокирует исходящие соединения, заявку в
   Telegram отправляет сам браузер посетителя — для этого ему нужен токен.
   Заявка при этом всё равно сохраняется на сервере (/api/leads.php) и
   уходит на почту, так что она не потеряется, даже если бот отключат.

   Как получить данные:
     1) @BotFather → /newbot → скопируйте токен;
     2) напишите своему боту любое сообщение;
     3) https://api.telegram.org/bot<ТОКЕН>/getUpdates → "chat":{"id":ЧИСЛО}.
   ===================================================================== */
window.NRD_TG = {
  token: '',   // '8812866039:AAG...'
  chat:  ''    // '8462836030'
};
